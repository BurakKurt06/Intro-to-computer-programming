 import javax.swing.JOptionPane;
// Header:
// Name:Burak Kurt
// Project name: Math Practice
// Date:11/12/2020
class Main {
  public static void main(String[] args) {
    //SQUARE
 /*  String input;
		 double square;
     
		 JOptionPane.showMessageDialog(null, " Hello, Let's calculate area of square! ");

		 input = JOptionPane.showInputDialog(" How long is one side of the square? ");
		 square = Double.parseDouble(input);
     square = (square * square);

		 JOptionPane.showMessageDialog(null, " Area of Square = "+ square +""); */
//==============================
   //CIRCLE
  /* String input;
     double circle, radius, PI=3.14; 
    
		 JOptionPane.showMessageDialog(null, " Hello, Let's calculate area of circle! ");

		 input = JOptionPane.showInputDialog(" How long is the radius? ");
		 radius = Double.parseDouble(input);
     circle = (PI * (radius * radius));
     JOptionPane.showMessageDialog(null, " Area of Circle = "+ circle +""); */
//==============================
    //TRIANGLE
  /* String input;
     double base, height, triangle; 
    
		 JOptionPane.showMessageDialog(null, " Hello, Let's calculate area of triangle! ");

		 input = JOptionPane.showInputDialog(" How long is the base? ");
		 base = Double.parseDouble(input);
     input = JOptionPane.showInputDialog(" How long is the height? ");
		 height = Double.parseDouble(input);     
     triangle = ((base * height)* 1/2);
     JOptionPane.showMessageDialog(null, " Area of Triangle = "+ triangle +""); */
//==============================
    //TRAPEZOID
  /* String input;
     double trapezoid, baseTOP, baseBOTTOM, height; 
    
		 JOptionPane.showMessageDialog(null, " Hello, Let's calculate area of Trapezoid ! ");
     
     input = JOptionPane.showInputDialog(" How long is the height? ");
		 height = Double.parseDouble(input);

		 input = JOptionPane.showInputDialog(" How long is the top line? ");
		 baseTOP = Double.parseDouble(input);

     input = JOptionPane.showInputDialog(" How long is the bottom line? ");
		 baseBOTTOM = Double.parseDouble(input); 

     trapezoid = ((height * (baseTOP + baseBOTTOM)) * 1/2);
     JOptionPane.showMessageDialog(null, " Area of Trapezoid = "+ trapezoid +""); */
  }
}